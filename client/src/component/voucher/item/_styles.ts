import styled from "styled-components";

export const StylesVoucherItem = styled.section`
  display: flex;

  .info-section {
    display: flex;
    flex-direction: column;
    margin: 0;
    flex-grow: 1;
  }

  .item-section {
    margin: 0;
    align-self: center;
  }
`