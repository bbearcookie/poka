import styled from 'styled-components';

export const StyledGroup = styled.tr`
  .name-section {
    width: fit-content;
    display: flex;
    align-items: center;
    gap: 1em;
    text-decoration: none;
    color: inherit;

    &:hover {
      text-decoration: underline;
    }
  }
`;
